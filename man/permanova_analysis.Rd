% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permanova_analysis.R
\name{permanova_analysis}
\alias{permanova_analysis}
\title{Permutational Multivariate Analysis of Variance (PERMANOVA)}
\usage{
permanova_analysis(
  data,
  group_col = 1,
  dist_method = "bray",
  transform = c("none", "hellinger", "log", "pa", "wisconsin"),
  permutations = 999,
  seed = 42
)
}
\arguments{
\item{data}{A \code{data.frame}, \code{matrix}, or \code{list} containing grouping
metadata columns and numeric species counts.}

\item{group_col}{Character or integer specifying the grouping metadata column (default: 1).}

\item{dist_method}{Character. Distance metric passed to \code{vegan::vegdist} (default: \code{"bray"}).}

\item{transform}{Character. Community data transformation via \code{vegan::decostand}:
\code{"none"} (default), \code{"hellinger"} (recommended), \code{"log"}, \code{"pa"}, or \code{"wisconsin"}.}

\item{permutations}{Integer. Number of Monte Carlo permutations for the test (default: 999).}

\item{seed}{Optional integer. Random seed for reproducible permutation results (default: 42).}
}
\value{
A list containing:
  \item{PERMANOVA_Table}{The ANOVA-style results table from \code{vegan::adonis2}.}
  \item{Cleaned_Data}{Data frame combining the cleaned grouping factor and community matrix.}
  \item{Distance_Method}{The dissimilarity metric used.}
}
\description{
Performs PERMANOVA (using \code{vegan::adonis2}) to test for statistical differences
in community composition between groups. Automatically cleans empty samples, validates
numeric matrices, supports ecological transformations, and handles flexible data ingestion.
}
