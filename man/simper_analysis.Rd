% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simper_analysis.R
\name{simper_analysis}
\alias{simper_analysis}
\title{Similarity Percentages (SIMPER) Analysis}
\usage{
simper_analysis(
  data,
  group_col = 1,
  top_n = 5,
  transform = c("none", "hellinger", "log", "pa", "wisconsin"),
  permutations = 999,
  seed = 42
)
}
\arguments{
\item{data}{A \code{data.frame}, \code{matrix}, or \code{list} where grouping
metadata is at the start, followed by numeric species counts.}

\item{group_col}{Character or integer specifying the grouping metadata column (default: 1).}

\item{top_n}{Integer. Number of top contributing species to display per comparison (default: 5).}

\item{transform}{Character. Optional pre-transformation via \code{vegan::decostand}:
\code{"none"} (default), \code{"hellinger"} (recommended), \code{"log"}, \code{"pa"}, or \code{"wisconsin"}.}

\item{permutations}{Integer. Number of permutations for p-value calculation (default: 999).}

\item{seed}{Optional integer. Random seed for reproducible permutations (default: 42).}
}
\value{
A list containing:
  \item{SIMPER_Object}{The raw \code{simper} object from vegan.}
  \item{Summary_Table}{Tidy summary data frame of top contributing species per group comparison.}
  \item{Plot}{A publication-ready \code{ggplot} bar chart of top contributors.}
}
\description{
Identifies which species primarily contribute to the differences between groups
using Bray-Curtis dissimilarity decomposition (\code{vegan::simper}).
Automatically cleans empty samples, validates missing values, supports ecological
transformations, and generates a publication-ready ggplot2 bar chart.
}
